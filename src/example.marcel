load stdlib as std;
load graphs as g;
load linalg;

int n;
int m;
int k;
int p;

mat<int, 3@3> A;
mat<int, p@m> B;

mat<int, k@n> C = linalg::inverse(A) * B;

claim p = 3;  # this crashes, the compiler knows p must be 3

while (x > 0) {
    print("x is positive");
    x = x - 1;
}

fn factorial(n: int) -> int {
    if (n == 0) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}
