load stdlib;
load graphs;
load linalg;

int x = 5;

graph<ud, w, int> G =
{
    node A;
    node B;
    node C;
    edge A - B = 6;
    edge A - C = x;
    edge B - C = 4;
    edge C - B = 2;
};

mat<sym> b = getAdj(G);

print(x/6);
