load stdlib;
load graphs;
load linalg;

mat<num, 3 @ 3> A = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

list<num> B = [10, 20, 30];

graph<u, w, num> G = [
    nodes: [A, B, C];
    edges: [A-B, B-C, C-A];
];

print(G.B.weight + det(A));
