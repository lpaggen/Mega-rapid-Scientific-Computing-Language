load stdlib;
load graphs;
load linalg;

num x = 10;

graph<d, w, num> G =
{
    node A = 5;
    node B = 3;
    node C = 8;
    edge A -> B = 2;
    edge B -> C = 4;
    edge A -> C = 1;
    node D = 50*x*63672;
    edge C -> D = 10;
};

print("Graph G:");
print(G);


print();

mat<num> M = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

print(M * M);
