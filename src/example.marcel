load stdlib;
load graphs;
load linalg;

mat<float> a = [[1, 2, 3], [4, 5, 6], [7, 8, 9]];

graph<w, d, float> G = {
    node A = 9.0;
    node B = 8.0;
    node C = 7.0;
    edge A -> B = 1.0;
    edge B -> C = 2.0;
    edge C -> A = 3.0;
};

print("Matrix a:");
print(a);


edge AB = G.AB;

print("Edge AB weight:");
print(AB.weight);
