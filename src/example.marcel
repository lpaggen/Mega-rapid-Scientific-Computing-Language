load stdlib;
load graphs;
load linalg;

mat<num> A = [
    [1, 2, 3],
    [4, 5, 6],
    [7, 8, 9]
];

graph<u, w, num> G = {
    node A;
    node B = T(A);
    edge A - B;
};

print(G.B.weight + det(A));
